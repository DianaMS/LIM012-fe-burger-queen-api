const {
  initAdmin,
  getUsers,
  getUser,
  postUser,
  putUser,
  deleteUser,
} = require('../../controller/users');

describe('getUsers', () => {
  it('should get users collection', (done) => {
    done();
  });
});
